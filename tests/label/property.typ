#set text(lang: "en")
#outline()
#set heading(numbering: "1.")
#set heading(numbering: "1.")
= Section One <id:sec:one>
Lets see if that works if we can link the section with its full name like:
#ref(label("id:sec:one")). Now we want a name custom name like: #link(label("id:sec:one"))[Goto Heading]
